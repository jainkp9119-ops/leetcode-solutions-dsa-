class Solution:
    def isValid(self, s: str) -> bool:
        stack=[]
        pairs = {
            ')':'(',
            ']':'[',
            '}':'{',
        }
        for char in s:
            if char in pairs:
                if stack:
                    top_element=stack.pop()
                else:
                    top_element='#'
                if pairs[char]!=top_element:
                    return False
            else:
                stack.append(char)
        return not stack
        